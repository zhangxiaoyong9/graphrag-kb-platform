"""MCP query server subpackage: expose KB search to AI agents over stdio.

Run with ``python -m kb_platform.mcp``. The server is a thin HTTP proxy to the
running KB Platform API; see ``server.py`` and ``__main__.py``.
"""
